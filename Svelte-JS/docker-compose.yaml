version: '3.9'

services:
  postgres:
    image: postgres
    restart: always
    ports:
      - 3040:5432
    volumes:
      - ~/apps/postgres-coordination:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=root
      - POSTGRES_USER=root
      - POSTGRES_DB=coordination_db